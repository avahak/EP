import { Typography } from "@mui/material";

const Menu = () => {
    return (
        <div>
            <Typography variant="h1">Testing Material UI</Typography>
        </div>
    );
}

export { Menu };